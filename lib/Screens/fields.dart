import 'package:flutter/material.dart';

class AllFields extends StatelessWidget {
  const AllFields({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
