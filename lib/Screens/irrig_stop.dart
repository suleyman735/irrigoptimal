import 'package:flutter/material.dart';

class IrrigStop extends StatelessWidget {
  const IrrigStop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(children: [Text('Irrig Stop')]),
    );
  }
}
